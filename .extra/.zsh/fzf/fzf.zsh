# TODO: install fzf extras with $(brew --prefix)/opt/fzf/install
# TODO: use ~/.fzf.zsh instead of these 2 files
# [ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
[ -f ~/.fzf/shell/key-bindings.zsh ] && source ~/.fzf/shell/key-bindings.zsh
[ -f ~/.fzf/shell/completion.zsh ] && source ~/.fzf/shell/completion.zsh

[ -f ~/.extra/.zsh/fzf/extra.zsh ] && source ~/.extra/.zsh/fzf/extra.zsh
